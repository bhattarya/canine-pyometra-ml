# URL of your deployed Gemini proxy (see proxy/README.md). Leave blank to run
# the predictor offline / deterministic-only — the chat panel then shows a
# "not configured" note and everything else works.
VITE_PROXY_URL=
