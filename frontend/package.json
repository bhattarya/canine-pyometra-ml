{
  "name": "pyometra-outcome-predictor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "React frontend for the canine pyometra treatment-success predictor",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "sync-model": "node scripts/sync-model.mjs",
    "predev": "npm run sync-model",
    "prebuild": "npm run sync-model",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "parity": "node scripts/parity-check.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vite-plugin-singlefile": "^2.0.3"
  }
}
